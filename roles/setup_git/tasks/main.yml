---

- name: Configure git editor
  git_config:
    name: core.editor
    scope: global
    value: nvim

- name: Configure git user
  git_config:
    name: user.name
    scope: global
    value: "André L. C. Moreira"

- name: Configure git email
  git_config:
    name: user.email
    scope: global
    value: andrelcmoreira@disroot.org

# TODO: clone via ssh
...
